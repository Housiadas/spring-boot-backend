@.agents/PROGRAMMING_PRINCIPLES.md

## Project specific rules
- Controllers own the mapping boundary, every method calls the appropriate mapper as the last step before returning. (use mapstructs)
- Create tests to verify the changes 
- Respect the programming principles
- Use your java skills in .claude/skills for coding

## Project verification
- Run `make format` to format the generated code (spotless)
- Run tests
